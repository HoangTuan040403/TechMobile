const ORDER_STATUS = {
  PENDING: "pending",
  PAID: "paid",
  SHIPPED: "shipped",
  COMPLETED: "completed",
  CANCELLED: "cancelled"
};

const ORDER_TYPE = {
  ONLINE: "online",
  INSTORE: "instore"
};

const STATUS_TRANSITIONS = {
  [ORDER_TYPE.ONLINE]: {
    [ORDER_STATUS.PENDING]: [ORDER_STATUS.PAID, ORDER_STATUS.CANCELLED],
    [ORDER_STATUS.PAID]: [ORDER_STATUS.SHIPPED, ORDER_STATUS.CANCELLED],
    [ORDER_STATUS.SHIPPED]: [ORDER_STATUS.COMPLETED, ORDER_STATUS.CANCELLED],
    [ORDER_STATUS.COMPLETED]: [],
    [ORDER_STATUS.CANCELLED]: []
  },
  [ORDER_TYPE.INSTORE]: {
    [ORDER_STATUS.PENDING]: [ORDER_STATUS.COMPLETED, ORDER_STATUS.CANCELLED],
    [ORDER_STATUS.COMPLETED]: [],
    [ORDER_STATUS.CANCELLED]: []
  }
};

module.exports = { ORDER_STATUS, ORDER_TYPE, STATUS_TRANSITIONS };
